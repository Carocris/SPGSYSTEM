<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SPG System</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SPGSYSTEM.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-dark bg-dark border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
                    <i class="fas fa-store me-2"></i>SPG System
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        @if (User.IsInRole("Admin") || User.IsInRole("Auditor"))
                        {
                            <li class="nav-item">
                                <a class="nav-link text-light" asp-area="" asp-controller="Home" asp-action="Index">
                                    <i class="fas fa-tachometer-alt me-1"></i>Dashboard
                                </a>
                            </li>
                        }
                        
                        @if (User.IsInRole("Admin"))
                        {
                            <!-- Dropdown Inventario - SOLO ADMIN -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-boxes me-1"></i>Inventario
                            </a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a class="dropdown-item" asp-controller="Products" asp-action="Index">
                                        <i class="fas fa-box me-2"></i>Productos
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" asp-controller="Categories" asp-action="Index">
                                        <i class="fas fa-tags me-2"></i>Categorías
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" asp-controller="Suppliers" asp-action="Index">
                                        <i class="fas fa-truck me-2"></i>Proveedores
                                    </a>
                                </li>
                            </ul>
                        </li>

                            <!-- Dropdown Ventas - SOLO ADMIN -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-shopping-cart me-1"></i>Ventas
                            </a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a class="dropdown-item" asp-controller="Sales" asp-action="Index">
                                        <i class="fas fa-receipt me-2"></i>Historial de Ventas
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" asp-controller="Sales" asp-action="CreateEdit">
                                        <i class="fas fa-plus me-2"></i>Nueva Venta
                                    </a>
                                </li>
                            </ul>
                        </li>

                            <!-- Clientes - SOLO ADMIN -->
                        <li class="nav-item">
                            <a class="nav-link text-light" asp-area="" asp-controller="Customers" asp-action="Index">
                                <i class="fas fa-users me-1"></i>Clientes
                            </a>
                        </li>

                            <!-- Pagos - SOLO ADMIN -->
                        <li class="nav-item">
                            <a class="nav-link text-light" asp-area="" asp-controller="Payments" asp-action="Index">
                                <i class="fas fa-credit-card me-1"></i>Pagos
                            </a>
                        </li>
                        }

                        @if (User.IsInRole("Auditor"))
                        {
                            <!-- Menú para Auditor - SOLO LECTURA -->
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="fas fa-chart-line me-1"></i>Reportes
                                </a>
                                <ul class="dropdown-menu">
                                    <li>
                                        <a class="dropdown-item" asp-controller="Products" asp-action="Index">
                                            <i class="fas fa-box me-2"></i>Productos (Solo Ver)
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="Categories" asp-action="Index">
                                            <i class="fas fa-tags me-2"></i>Categorías (Solo Ver)
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="Sales" asp-action="Index">
                                            <i class="fas fa-receipt me-2"></i>Ventas (Solo Ver)
                                        </a>
                                    </li>
                                </ul>
                            </li>
                        }

                        @if (User.IsInRole("Supplier"))
                        {
                            <!-- Menú para Proveedores -->
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="fas fa-boxes me-1"></i>Mis Productos
                                </a>
                                <ul class="dropdown-menu">
                                    <li>
                                        <a class="dropdown-item" asp-controller="SupplierPortal" asp-action="Index">
                                            <i class="fas fa-box me-2"></i>Ver Productos
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="Products" asp-action="Create">
                                            <i class="fas fa-plus me-2"></i>Agregar Producto
                                        </a>
                                    </li>
                                </ul>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-light" asp-area="" asp-controller="Notifications" asp-action="Index">
                                    <i class="fas fa-bell me-1"></i>Notificaciones
                                </a>
                            </li>
                        }

                        @if (User.IsInRole("Customer"))
                        {
                            <!-- Menú para Clientes -->
                            <li class="nav-item">
                                <a class="nav-link text-light" asp-area="" asp-controller="CustomerPortal" asp-action="Index">
                                    <i class="fas fa-shopping-bag me-1"></i>Catálogo
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-light" asp-area="" asp-controller="CustomerPortal" asp-action="MyOrders">
                                    <i class="fas fa-shopping-cart me-1"></i>Mis Compras
                                </a>
                            </li>
                        }
                    </ul>

                    <!-- Usuario y Autenticación -->
                    <ul class="navbar-nav">
                        @if (User.Identity?.IsAuthenticated == true)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="fas fa-user me-1"></i>@User.Identity.Name
                                    <span class="badge bg-secondary ms-1">
                                        @if (User.IsInRole("Admin")) { <text>Admin</text> }
                                        else if (User.IsInRole("Auditor")) { <text>Auditor</text> }
                                        else if (User.IsInRole("Supplier")) { <text>Proveedor</text> }
                                        else if (User.IsInRole("Customer")) { <text>Cliente</text> }
                                    </span>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <span class="dropdown-item-text">
                                            <i class="fas fa-user-circle me-2"></i>@User.FindFirst("FullName")?.Value
                                        </span>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <form asp-controller="Account" asp-action="Logout" method="post" style="display: inline;">
                                            <button type="submit" class="dropdown-item">
                                                <i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión
                                            </button>
                                        </form>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link text-light" asp-controller="Account" asp-action="Login">
                                    <i class="fas fa-sign-in-alt me-1"></i>Iniciar Sesión
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <div class="container-fluid px-3 px-md-4">
        <!-- Notifications Section -->
        <div id="notifications-container" class="position-fixed top-0 end-0 p-3" style="z-index: 1050;">
            <!-- Notifications will be dynamically added here -->
        </div>

        <!-- Simple Notification System -->
        <script>
            const NotificationSystem = {
                show: function(message, type = 'info', duration = 5000) {
                    const container = document.getElementById('notifications-container');
                    const notificationId = 'notification-' + Date.now();
                    
                    const alertClass = type === 'success' ? 'alert-success' : 
                                     type === 'error' ? 'alert-danger' : 
                                     type === 'warning' ? 'alert-warning' : 'alert-info';
                    
                    const icon = type === 'success' ? 'fas fa-check-circle' :
                                type === 'error' ? 'fas fa-exclamation-circle' :
                                type === 'warning' ? 'fas fa-exclamation-triangle' : 'fas fa-info-circle';
                    
                    const notification = document.createElement('div');
                    notification.id = notificationId;
                    notification.className = `alert ${alertClass} alert-dismissible fade show`;
                    notification.style.minWidth = '300px';
                    notification.innerHTML = `
                        <div class="d-flex align-items-center">
                            <i class="${icon} me-2"></i>
                            <span>${message}</span>
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    `;
                    
                    container.appendChild(notification);
                    
                    // Auto-remove after duration
                    setTimeout(() => {
                        this.hide(notificationId);
                    }, duration);
                },
                
                hide: function(notificationId) {
                    const notification = document.getElementById(notificationId);
                    if (notification) {
                        notification.remove();
                    }
                },
                
                hideAll: function() {
                    const container = document.getElementById('notifications-container');
                    container.innerHTML = '';
                }
            };
        </script>

        <!-- Main Content -->
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container-fluid">
            &copy; 2025 - SPG System
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
    @await Html.PartialAsync("_NotificationHelper")
</body>
</html>
